import React from "react";

export default function Dashboard(){
    return(<div className="box">
        <div>Dashboard</div>
        <p>/dashboard in title bar shows Dashboard</p>
        <p>/create-user in title bar shows create user and list user</p>
    </div>)
}